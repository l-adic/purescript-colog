{
  "name": "purescript-colog",
  "version": "0.0.0",
  "private": true,
  "description": "A PureScript port of the colog logging library",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "repl": "spago repl",
    "bundle": "spago bundle"
  },
  "devDependencies": {
    "purescript": "0.15.16",
    "spago": "0.93.45"
  }
}
